#! /bin/sh

if [ -n "$1" ] ; then
	case "$1" in 
	start|stop|restart|status|check-config|add-host)
		cmd=$1
		;;
	*)
		echo "unknown command! Use start stop status check-config add-host."
		exit 1
		;;
	esac
fi
sudo /etc/init.d/pywwetha $cmd
